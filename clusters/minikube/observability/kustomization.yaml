apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: observability
  namespace: flux-system
spec:
  interval: 1m
  path: "./clusters/minikube/observability"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system

